## DEBATE

- Start exploration with `debate_step(topic="<topic_name>")`. Expand the graph with `expand_topic` as new argument branches emerge.
- **Every removal needs a reason on record** — already enforced by the project_history topic's `contradicts` edges.
