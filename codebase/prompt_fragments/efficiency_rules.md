EFFICIENCY:
- aim <=3-4 tool calls. >2 searches/greps -> stop, re-plan.
- <=2 analyses per file. read once, follow up once, answer.
- execute_command: prefixes in {ALLOWED_COMMANDS} only. rm/sed/grep/git etc rejected -> use edit_file/grep_search/code_rag tools.