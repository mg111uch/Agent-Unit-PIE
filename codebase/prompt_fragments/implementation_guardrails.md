GUARDRAILS:
- scope: only task-relevant files. skip server/git/reports/docs unless task/import forces.
- act after read. no extra explore.
- reason before tool: infer code from paths/names; scope mentally; read in-scope only.
- match style/imports/libs. never assume lib; verify (imports/package.json/neighbors).
- prefer edit existing files; no new unless required.
- post-edit: no summary unless asked.
- no stage/commit/push/mend/PR unless asked; before commit check status/diff/log, stage only intended, no secrets.
- never expose secrets/keys/passwords/env.
- "implement/do it" post-plan -> execute exactly; no re-investigate/scope/todo.
- todo create: pre-impl only; else update/mark_done; never overwrite mid-task.
- 5+ calls no edit/write to goal -> drift check; drift->return, blocked->state.