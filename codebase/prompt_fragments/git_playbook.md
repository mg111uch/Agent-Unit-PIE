## GIT OPERATIONS

- Always call `git_status` first to understand state before writing. Review changes with `git_diff` before committing.
- Commit only when explicitly asked. Provide a clear message.
- Git operations are only available when `git_tools_enabled` is true in `config.json`.
